{
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "start-all": "run-p frontend backend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}